---
import "@fontsource-variable/nunito";
import Note from "../components/icons/Note.astro";

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<Note class="note" />
		<slot />
		<Note />
	</body>
	<script>
		// const $body = document.querySelector("body");
		// const symbols = ["♪", "♫", "♬", "🤖"];

		// function createSymbol() {
		// 	const symbol = document.createElement("svg");
		// 	symbol.className = Math.random() < 0.5 ? "note" : "robot";
		// 	symbol.innerText = symbols[Math.floor(Math.random() * symbols.length)];
		// 	symbol.style.left = Math.random() * 95 + "vw";
		// 	symbol.style.animationDuration = 3 + Math.random() * 10 + "s";
		// 	$body.appendChild(symbol);

		// 	symbol.addEventListener("animationend", () => {
		// 		symbol.remove();
		// 	});
		// }

		// setInterval(createSymbol, 3000);
	</script>
</html>
<style is:global>
	* {
		box-sizing: border-box;
	}
	:root {
		--celeste: #c5fffdff;
		--black: #07020dff;
		--khaki: #a39b8bff;
		--dim-gray: #716a5cff;
		--savoy-blue: #6369d1ff;
	}
	html {
		font-family: "Nunito Variable", sans-serif;
		background: var(--black);
	}
	body {
		margin: 0;
		padding: 0;
		width: 100%;
		height: 100vh;
		display: flex;
		flex-direction: column;
		align-items: center;
		padding: 0px 50px;
	}

	.note,
	.robot {
		position: absolute;
		top: 0px; /* Empieza fuera de la vista */
		width: 40px; /* Ajusta el tamaño del SVG */
		height: 40px;
		animation: fall infinite linear 5s; /* Animación de caída */
		z-index: -1;
	}

	@keyframes fall {
		to {
			transform: translateY(90vh); /* Termina en la parte inferior de la pantalla */
		}
	}
</style>
