<section id="formMusic">
    <h1>Choose your preferences and let´s create a new sound</h1>
    <div class="lines">
        <div class="line"></div>
        <div class="line"></div>
        <div class="line"></div>
        <div class="line"></div>
        <div class="line"></div>
        <div class="line"></div>
    </div>
    <form action="submit">
        <label for="feeling"
            >What kind of feeling would you like to express?</label
        >
        <input type="text" id="feeling" name="feeling" />
        <label for="genre"
            >What genre would you like to apply for your music?</label
        >
        <input type="text" id="genre" name="genre" />
        <label for="instrument">What instrument would you like to use?</label>
        <input type="text" id="instrument" name="instrument" />
        <button type="submit">Submit</button>
    </form>
</section>

<style>
    section {
        margin-top: 100px;
        width: 100%;
        color: var(--celeste);
        position: relative;
    }

    h1 {
        font-size: 2rem;
        text-align: center;
    }

    form {
        width: 30rem;
        height: 22rem;
        box-shadow: 0 0 1rem 0 #00000033;
        border-radius: 5px;
        background-color: #6369d115;
        backdrop-filter: blur(5px);
        display: grid;
        grid-template-columns: 1fr;
        grid-template-rows: 40px 30px 40px 30px 40px 30px 50px;
        padding: 10px 30px;
        gap: 10px;
    }

    label {
        font-size: 1rem;
        margin-bottom: 0px;
        height: fit-content;
        margin-top: 20px;
    }

    button {
        background-color: #2b3c55;
        color: var(--celeste);
        font-size: 1rem;
        padding: 0.5rem 1rem;
        border-radius: 5px;
        cursor: pointer;
        border: none;
        width: 150px;
        justify-self: center;
        margin-top: 20px;
        font-family: "Nunito Variable", sans-serif;
        height: 40px;
    }

    input{
        background-color: transparent;
        border-width: 0px 0px 1px 0px;
        width: 80%;
        color: var(--celeste);
    }

    input:focus{
        outline: none;
    }

    .lines {
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 100%;
        width: 20vw;
    }

    .line {
        position: absolute;
        width: 1px;
        height: 100%;
        top: 20px;
        left: 10%;
        background: #ffffff1a;
        overflow: hidden;
    }

    .line::after {
        content: "";
        display: block;
        position: absolute;
        height: 15vh;
        width: 100%;
        top: -50%;
        left: 0;
        background: linear-gradient(
            to bottom,
            #ffffff00 0%,
            #ffffff 75%,
            #ffffff 100%
        );
        animation: drop 7s 0s infinite;
        animation-fill-mode: forwards;
        animation-timing-function: cubic-bezier(0.4, 0.26, 0, 0.97);
    }

    .line:nth-child(1) {
        margin-left: -25%;
    }

    .line:nth-child(1)::after {
        animation-delay: 2s;
    }

    .line:nth-child(3) {
        margin-left: 25%;
    }

    .line:nth-child(3)::after {
        animation-delay: 2.5s;
    }

    .line:nth-child(5) {
        margin-left: 50%;
    }

    .line:nth-child(5)::after {
        animation-delay: 3s;
    }

    .line:nth-child(2) {
        margin-left: -50%;
    }

    .line:nth-child(2)::after {
        animation-delay: 3.5s;
    }

    .line:nth-child(4) {
        margin-left: 75%;
    }

    .line:nth-child(4)::after {
        animation-delay: 4s;
    }

    @keyframes drop {
        0% {
            top: -50%;
        }
        100% {
            top: 110%;
        }
    }
</style>
